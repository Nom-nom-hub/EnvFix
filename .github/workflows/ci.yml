name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        go-version: ['1.21', '1.25']

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Go environment
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Install dependencies
      run: go mod tidy && go mod download

    - name: Run linter (golangci-lint)
      uses: golangci/golangci-lint-action@v3
      with:
        version: latest
        args: --timeout=5m
      continue-on-error: true

    - name: Run tests
      run: go test -v -race -coverprofile=coverage.out ./internal/detector ./internal/parser ./internal/rebuilder ./internal/utils ./internal/config ./internal/cleaner ./internal/spec

    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        files: ./coverage.out
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

    - name: Build binaries
      run: |
        go build -o envfix-${{ matrix.os }} -v

  quality:
    runs-on: ubuntu-latest
    needs: build

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Go environment
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'

    - name: Run go vet
      run: go vet ./...

    - name: Run go fmt check
      run: |
        if [ $(gofmt -l . | wc -l) -gt 0 ]; then
          echo "Code formatting issues found. Run 'go fmt ./...'"
          gofmt -l .
          exit 1
        fi

    - name: Check for TODO/FIXME comments
      run: |
        grep -r "TODO\|FIXME" . --include="*.go" || true

  test-coverage:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Go environment
      uses: actions/setup-go@v4
      with:
        go-version: '1.25'

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Install dependencies
      run: go mod tidy && go mod download

    - name: Generate coverage report
      run: go test -v -coverprofile=coverage.out -covermode=atomic ./internal/detector ./internal/parser ./internal/rebuilder ./internal/utils ./internal/config ./internal/cleaner ./internal/spec

    - name: Check coverage threshold (25%)
      run: |
        COVERAGE=$(go tool cover -func=coverage.out | grep total | awk '{print $3}' | sed 's/%//')
        echo "Coverage: $COVERAGE%"
        if (( $(echo "$COVERAGE < 25" | bc -l) )); then
          echo "Coverage below 25% threshold"
          exit 1
        fi

    - name: Upload coverage report
      uses: actions/upload-artifact@v4
      with:
        name: coverage-report
        path: coverage.out
        retention-days: 5
